# Hooks

No lifecycle hook is enabled in the minimal plugin.

Codex automatically discovers plugin hook files named `hooks/hooks.json`, so this directory intentionally does not include one. Add a native Codex hook only after the behavior is safe enough to trust and review.
