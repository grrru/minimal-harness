interface:
  display_name: "Grill"
  short_description: "Stress-test a plan by interviewing the user"
  default_prompt: "Use $grill to interview me about this plan until we understand it the same way."

policy:
  allow_implicit_invocation: false
